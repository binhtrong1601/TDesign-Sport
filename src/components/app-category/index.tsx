import { Divider, Checkbox, Row, Col } from 'antd';
import { CheckboxChangeEvent, CheckboxProps } from 'antd/es/checkbox/Checkbox';

// const checkOption = ['Customizable', 'Designable', '#FFFFF', 'XS'];
const AppCategory = ({ ...props }: CheckboxProps) => {
  return (
    <div>
      <Checkbox></Checkbox>
    </div>
  );
};
export default AppCategory;
