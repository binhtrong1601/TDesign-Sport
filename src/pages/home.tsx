import { AppButton, AppSelect } from '../components';

const HomePage = () => {
  return (
    <div className="App">
      <p>Home page</p>
      <AppButton />
      <AppSelect />
    </div>
  );
};

export default HomePage;
